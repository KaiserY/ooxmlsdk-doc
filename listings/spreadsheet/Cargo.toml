[package]
name = "ooxmlsdk-doc-spreadsheet"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
ooxmlsdk.workspace = true

[dev-dependencies]
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }
